# AGENTS

## 目标

在本仓库内协作完成一个基于 Eino 的业务背景智能助手，优先保证可解释、可追溯、可迭代。

## 工作范围

- 主要开发目录：`/Users/zhouhuaifeng/Code/work/xunlei/ssp/agent_biz`
- 参考资料目录：`docs/`
- 默认不修改外部知识仓库内容，除非用户明确要求。

## 资料优先级

1. `docs/rc_context`：历史需求文档（主要是 PDF）。
2. `work_security_skills`：业务规则、口径、schema、历史知识。
3. `ssp` 代码仓库：实现线索与上下文补充。
4. `docs/eino/*`：Eino 框架实现与示例参考。

## 执行边界

- V1 以“背景解释”为主，不默认做全自动化执行。
- 没有来源证据时，不把模型推断当成业务事实。
- 未经用户明确要求，不执行外部数据查询。
- 未经用户明确要求，不写回外部知识库。

## 协作约定

1. 优先小步改动，保持易审阅和易回滚。
2. 代码行为变化时，同步更新必要文档。
3. 多条指令冲突时，以最新用户指令为准。

